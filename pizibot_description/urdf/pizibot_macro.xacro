<?xml version="1.0"?>
<robot xmlns:xacro="http://www.ros.org/wiki/xacro">

    <xacro:macro name="pizibot">

        <!-- BASE LINK -->
        <link name="base_link"></link>

        <!-- BASE LINK FOOTPRINT -->
        <link name="base_footprint"></link>

        <joint name="base_footprint_joint" type="fixed">
            <parent link="base_link"/>
            <child link="base_footprint"/>
            <origin xyz="0 0 -${ground_clearance/2}" rpy="0 0 0"/>
        </joint>

        <!-- CHASSIS -->
        <joint name="chassis_joint" type="fixed">
            <parent link="base_link"/>
            <child link="chassis"/>
            <origin xyz="0 0 0" rpy="0 0 -${pi/2}"/>
        </joint>

        <link name="chassis">
            <collision>
                <origin xyz="0 0 ${robot_height/2}" rpy="0 0 0"/>
                <geometry>
                    <cylinder length="${robot_height}" radius="${robot_diameter/2}" />
                </geometry>
            </collision>

            <xacro:inertial_cylinder mass="${robot_mass}" length="${robot_height}" radius="${robot_diameter/2}">
                <origin xyz="0 0 ${robot_height/2}" rpy="0 0 0"/>
            </xacro:inertial_cylinder>
        </link>

        <joint name="chassis_visual_joint" type="fixed">
            <parent link="base_link"/>
            <child link="chassis_visual"/>
            <origin xyz="0 0 0" rpy="${pi/2} 0 ${pi/2}"/>
        </joint>

        <link name="chassis_visual">
            <visual>
                <origin xyz="0 0 0" rpy="0 0 0 "/>
                <geometry>
                    <mesh filename="package://pizibot_description/meshes/robot_chassis.stl" scale="0.001 0.001 0.001"/>
                </geometry>
                <material name="white"/>
            </visual>
        </link>

        <gazebo reference="chassis_visual">
            <material>Gazebo/White</material>
        </gazebo>

        <!-- left_wheel -->
        <joint name="left_wheel_joint" type="continuous">
            <parent link="chassis"/>
            <child link="left_wheel"/>
            <origin xyz="${-((robot_diameter/2)-(wheel_width/2))} 0 -${wheel_y_offset}" rpy="-${pi/2} 0 ${pi/2}"/>
            <axis xyz="0 0 1"/>
            <limit effort="${max_wheel_effort}" velocity="${max_wheel_velocity}"/>
        </joint>

        <link name="left_wheel">
            <visual>
                <origin xyz="0 0 0" rpy="0 0 0"/>
                <geometry>
                    <mesh filename="package://pizibot_description/meshes/wheel.stl" scale="0.001 0.001 0.001"/>
                </geometry>
                <material name="red"/>
            </visual>

            <collision>
                <geometry>
                    <cylinder length="${wheel_width}" radius="${wheel_diameter/2}" />
                </geometry>
            </collision>

            <xacro:inertial_cylinder mass="${wheel_mass}" length="${wheel_width}" radius="${wheel_diameter/2}">
                <origin xyz="0 0 0" rpy="0 0 0"/>
            </xacro:inertial_cylinder>
        </link>


        <gazebo reference="left_wheel">
            <material>Gazebo/Red</material>
        </gazebo>


        <!-- right_wheel -->
        <joint name="right_wheel_joint" type="continuous">
            <parent link="chassis"/>
            <child link="right_wheel"/>
            <origin xyz="${((robot_diameter/2)-(wheel_width/2))} 0 -${wheel_y_offset}" rpy="-${pi/2} 0 -${pi/2}"/>
            <axis xyz="0 0 -1"/>
            <limit effort="${max_wheel_effort}" velocity="${max_wheel_velocity}"/>
        </joint>

        <link name="right_wheel">
            <visual>
                <geometry>
                    <mesh filename="package://pizibot_description/meshes/wheel.stl" scale="0.001 0.001 0.001"/>
                </geometry>
                <material name="red"/>
            </visual>

            <collision>
                <geometry>
                    <cylinder length="${wheel_width}" radius="${wheel_diameter/2}" />
                </geometry>
            </collision>

            <xacro:inertial_cylinder mass="${wheel_mass}" length="${wheel_width}" radius="${wheel_diameter/2}">
                <origin xyz="0 0 0" rpy="0 0 0"/>
            </xacro:inertial_cylinder>
        </link>

        <gazebo reference="right_wheel">
            <material>Gazebo/Red</material>
        </gazebo>



        <!-- FRONT CASTER WHEEL -->
        <joint name="front_caster_joint" type="fixed">
            <parent link="chassis"/>
            <child link="front_caster"/>
            <origin xyz="0 ${caster_x_offset} -${ground_clearance}" rpy="0 0 0"/>
        </joint>

        <link name="front_caster">
            <visual>
                <geometry>
                    <sphere radius="${caster_diameter/2}" />
                </geometry>
                <material name="black"/>
            </visual>

            <collision>
                <geometry>
                    <sphere radius="${caster_diameter/2}" />
                </geometry>
            </collision>

            <xacro:inertial_sphere mass="${caster_mass}" radius="${caster_diameter/2}">
                <origin xyz="0 0 0" rpy="0 0 0"/>
            </xacro:inertial_sphere>
        </link>

        <gazebo reference="front_caster">
            <material>Gazebo/Black</material>
            <mu1 value="0.001"/>
            <mu2 value="0.001"/>
        </gazebo>


        <!-- BACK CASTER WHEEL -->
        <joint name="back_caster_joint" type="fixed">
            <parent link="chassis"/>
            <child link="back_caster"/>
            <origin xyz="0 -${caster_x_offset} -${ground_clearance}" rpy="0 0 0"/>
        </joint>

        <link name="back_caster">
            <visual>
                <geometry>
                    <sphere radius="${caster_diameter/2}" />
                </geometry>
                <material name="black"/>
            </visual>

            <collision>
                <geometry>
                    <sphere radius="${caster_diameter/2}" />
                </geometry>
            </collision>

            <xacro:inertial_sphere mass="${caster_mass}" radius="${caster_diameter/2}">
                <origin xyz="0 0 0" rpy="0 0 0"/>
            </xacro:inertial_sphere>
        </link>
        <gazebo reference="back_caster">
            <material>Gazebo/Black</material>
            <mu1 value="0.001"/>
            <mu2 value="0.001"/>
        </gazebo>

        <!-- FRONT CASTER CYLINDER -->
        <joint name="front_caster_cylinder_joint" type="fixed">
            <parent link="chassis"/>
            <child link="front_caster_cylinder"/>
            <origin xyz="0 ${caster_x_offset} 0" rpy="0 0 0"/>
        </joint>

        <link name="front_caster_cylinder">
            <collision>
                <origin xyz="0 0 -${ground_clearance/2}" rpy="0 0 0 "/>
                <geometry>
                    <cylinder length="${ground_clearance}" radius="${caster_diameter/2}" />
                </geometry>
            </collision>
        </link>

        <!-- BACK CASTER CYLINDER -->
        <joint name="back_caster_cylinder_joint" type="fixed">
            <parent link="chassis"/>
            <child link="back_caster_cylinder"/>
            <origin xyz="0 -${caster_x_offset} 0" rpy="0 0 0"/>
        </joint>

        <link name="back_caster_cylinder">
            <collision>
                <origin xyz="0 0 -${ground_clearance/2}" rpy="0 0 0 "/>
                <geometry>
                    <cylinder length="${ground_clearance}" radius="${caster_diameter/2}" />
                </geometry>
            </collision>
        </link>

    </xacro:macro>
</robot>
